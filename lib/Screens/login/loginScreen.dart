import 'package:flutter/material.dart';
import 'package:mysql_login/Screens/login/components/login.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Login(),
    );
  }
}